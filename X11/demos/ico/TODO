- Add a demo switch with some prepackaged neat demos
- Add a verify switch which makes sure that all the counts in
  the objects are less than the MAX values defined in the program
- Add a switch to tell how to use the colors on the faces
