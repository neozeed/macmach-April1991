           SRCS = ttextext.c mapfile.c 
           OBJS = ttextext.o mapfile.o 
LOCAL_LIBRARIES = $(XLIB)
        DEPLIBS = $(DEPXLIB)

AllTarget(ttextext)
SingleProgramTarget(ttextext,$(OBJS),$(LOCAL_LIBRARIES), /**/)
DependTarget()
